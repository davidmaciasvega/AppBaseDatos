services:
  - type: web
    name: laravel-backend
    runtime: docker
    rootDir: backend-laravel
    plan: free
    envVars:
      - key: APP_ENV
        value: "production"
      - key: APP_DEBUG
        value: "false"
      - key: APP_KEY
        sync: false
